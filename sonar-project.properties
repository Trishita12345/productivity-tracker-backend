# must be unique in a given SonarQube instance
sonar.projectKey=productivity-tracker-backend

# --- optional properties ---

# defaults to project key
sonar.projectName=productivity-tracker-backend
# defaults to 'not provided'
sonar.projectVersion=1.0
 
# Path is relative to the sonar-project.properties file. Defaults to .
sonar.sources=app

# Path is for locate test files
# sonar.tests=*
 
# Exclude this files to QA
# sonar.exclusions=/node_modules/*,/coverage/lcov-report/*,test/*,.*,ER-diagram.erdplus,

# Coverage Reporting
# sonar.javascript.lcov.reportPaths=./coverage/lcov.info

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8